import styles from "../styles.module.css";


export const Negative = () => {
  return (
    <div
      className={`bg-lightgrey ${styles["ribbon"]}`}
    >
      <p className="bold">Negative</p>
    </div>
  );
};