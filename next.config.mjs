/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "gdb.voanews.com",
      "payloadspace.com",
      "sm.mashable.com",
    ],
  },
};

export default nextConfig;
